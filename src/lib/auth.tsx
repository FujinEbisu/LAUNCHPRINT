// Placeholder auth export. The original implementation depended on 'better-auth',
// which is not installed. Since this module is not imported anywhere else,
// we export a minimal stub to satisfy the type checker.

export const auth = {} as const;
